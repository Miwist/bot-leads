FROM node:20-alpine
# ffmpeg нужен для распознавания голосовых (OGG→WAV) в Telegram, как в Dockerfile
RUN apk add --no-cache wget ffmpeg
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
EXPOSE 3001
CMD ["npm", "run", "dev"]
